SELECT CITY, LENGTH(CITY) FROM (SELECT * FROM STATION ORDER BY LENGTH(CITY), CITY ASC)
WHERE ROWNUM = 1; 

SELECT CITY, LENGTH(CITY) FROM (SELECT * FROM STATION ORDER BY  LENGTH(CITY)DESC, CITY)
WHERE ROWNUM = 1;
